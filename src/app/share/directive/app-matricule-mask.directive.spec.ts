import { AppMatriculeMaskDirective } from './app-matricule-mask.directive';

describe('AppMatriculeMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMatriculeMaskDirective();
    expect(directive).toBeTruthy();
  });
});
